const knowledgeBase = {
  name: "Abubakkar",
  fullName: "Abubakkar",
  title: "Full-Stack Developer & Creative Problem Solver",
  location: "Remote / Worldwide",
  email: "hello@Abubakkar.dev",
  socials: {
    github: "github.com/Abubakkar",
    linkedin: "linkedin.com/in/Abubakkar",
    twitter: "@Abubakkar_dev"
  },
  about: [
    "I'm a full-stack developer with 4+ years of experience building fast, intuitive digital products.",
    "I specialize in React, Node.js, TypeScript, and modern web technologies.",
    "I'm passionate about performance, clean code, and bold design.",
    "When not coding, I explore new frameworks, contribute to open-source, and perfect my pour-over coffee."
  ],
  skills: {
    frontend: ["React", "Next.js", "TypeScript", "CSS/Sass", "Tailwind"],
    backend: ["Node.js", "Python", "PostgreSQL", "MongoDB", "Redis"],
    devops: ["Docker", "AWS", "CI/CD", "Kubernetes"],
    mobile: ["React Native"],
    tools: ["Git", "WebSocket", "WebRTC"]
  },
  experience: [
    {
      role: "Senior Full-Stack Developer",
      company: "TechCorp Inc.",
      period: "2024 — Present",
      description: "Leading development of microservices architecture serving 1M+ users. Built real-time analytics dashboard and CI/CD pipeline."
    },
    {
      role: "Full-Stack Developer",
      company: "StartupXYZ",
      period: "2022 — 2024",
      description: "Developed core product features using React, Node.js, and MongoDB. Reduced load times by 40% and led 3 major releases."
    },
    {
      role: "Junior Developer",
      company: "WebAgency Co.",
      period: "2020 — 2022",
      description: "Built 15+ client websites and web apps. Migrated legacy jQuery codebase to React."
    }
  ],
  projects: [
    {
      name: "Black Wolf Media",
      tagline: "Expert Video Editing Services Lahore, Pakistan",
      description: "A full-service video editing and production agency website built for Lahore, Pakistan. Features cinematic portfolio showcase, client testimonials, service booking, and AI-powered content solutions.",
      longDescription: "Black Wolf Media is Lahore's trusted video editing agency and video production agency. Founded in 2019, they deliver cinematic-quality content for businesses across Pakistan. From Lahore and Karachi to Sharjah and beyond, they transform raw ideas into high-impact visual stories that build brand authority, drive qualified leads, and dominate competitive markets. With over 500 completed projects and 6+ years in business, they partner with startups, SMEs, enterprises, and Pakistan government entities.",
      tech: ["Next.js", "React", "Tailwind", "Web Design"],
      url: "https://blackwolfmediame.vercel.app",
      services: ["Brand Identity", "Video Production", "Web Design", "Social Media", "Content Creation", "AI Solutions"],
      stats: { projects: "500+", years: "6+", clients: "50+" },
      location: "Lahore / Islamabad, Pakistan",
      founded: 2019,
      pricing: "PKR 5,000 to PKR 50,000+",
      turnaround: "First drafts within 48-72 hours",
      languages: "Urdu & English bilingual content",
      industries: ["Real estate", "Luxury hospitality", "Tech startups"],
      email: "hello@blackwolfmedia.me",
      testimonials: [
        { name: "Sara Sadik", role: "Creative Director", text: "One of the most supportive and professional filming and editing teams I've ever worked with." },
        { name: "Natasha Nickels", role: "Marketing Manager", text: "Very thorough and professional team. Love their ideas." },
        { name: "Amita Dubey", role: "Brand Strategist", text: "Love their work, the quality is just amazing!" },
        { name: "Ahmed Al Rashed", role: "CEO, TechStart Pakistan", text: "Exceptional video content that captured our brand essence perfectly." }
      ],
      journey: [
        { year: 2019, milestone: "The Beginning — Founded with a vision to transform how Pakistan brands tell stories through video." },
        { year: 2022, milestone: "100+ Projects — Reached 100 completed projects and expanded the team of senior editors and cinematographers." },
        { year: 2024, milestone: "Full-Service Agency — Expanded into web design, social media management, and AI-powered content solutions." },
        { year: 2026, milestone: "500+ & Growing — Trusted partner for startups, SMEs, enterprises, and Pakistan government entities across South Asia." }
      ]
    },
    {
      name: "Zyro",
      tagline: "Launch Your Website in Hours, Not Days",
      description: "AI-powered web development platform that lets businesses launch websites in hours. Features conversion architecture, business automation, payment infrastructure, and custom SaaS solutions.",
      longDescription: "Zyro is an AI-powered web development platform that helps businesses launch websites in hours instead of days. They engineer systems that convert, scale, and perform — no bloat, no templates, just precision code. Their tagline is 'Building delightful product experiences & scalable platforms.' They offer services including Web Development, Automation, SaaS building, and Consulting. Their process follows a 3-phase approach: Discovery & Architecture, Precision Engineering, and Performance & Launch.",
      tech: ["Next.js", "TypeScript", "React", "Node.js", "MongoDB", "PostgreSQL", "Supabase", "Tailwind CSS", "Vercel"],
      url: "https://zyro-steel.vercel.app",
      services: ["Web Development", "Automation", "SaaS", "Consulting"],
      whatWeBuild: [
        { name: "Conversion Architecture", desc: "Engineering sales funnels on Next.js for instant load times that convert cold traffic into high-ticket clients." },
        { name: "Platform Migration", desc: "Moving businesses from fragile WordPress or Wix templates to secure, custom code environments that scale." },
        { name: "Business Automation", desc: "Integrating AI and n8n workflows to handle lead capture, email sequences, and client onboarding automatically." },
        { name: "Payment Infrastructure", desc: "Custom Stripe and subscription architectures for secure, complex billing flows handling global payments." },
        { name: "Performance Engineering", desc: "Optimizing every line of code to achieve 100/100 Core Web Vitals for faster load times than competitors." },
        { name: "Custom SaaS Solutions", desc: "Architecting scalable web applications from MVP to Enterprise launch using modern cloud technology." }
      ],
      process: [
        { phase: 1, name: "Discovery & Architecture", desc: "Auditing current bottlenecks, analyzing data, mapping user journeys, and architecting database structures that support business goals." },
        { phase: 2, name: "Precision Engineering", desc: "Building from scratch using Next.js and TypeScript with secure payments (Stripe), custom automation (n8n), and robust backends. No bloatware, no heavy plugins." },
        { phase: 3, name: "Performance & Launch", desc: "Optimizing every asset to achieve 100/100 Core Web Vitals. Ensuring SEO-ready, accessible, instantly loading on mobile devices. Deploying to Vercel or custom VPS." }
      ]
    },
    {
      name: "GrimZone",
      tagline: "Pakistan's #1 Free Fire Tournament Platform",
      description: "Pakistan's premier Free Fire tournament platform. Players compete in 1v1 and 2v2 tournaments, earn tokens, and win real prizes with a live world chat feature.",
      longDescription: "GrimZone is Pakistan's premier Free Fire tournament platform where players can compete, win, and dominate. With 348+ active players, 4 active tournaments, and 50K+ in prizes awarded, it has a 4.9 player rating. Players enter tournaments using 50 FF (Free Fire currency) and compete for prize pools of 80 FF. The platform features a slot system showing filled vs total slots, a World Chat for community interaction, and a Rules page for guidelines.",
      tech: ["Next.js", "React", "Real-time", "Gaming"],
      url: "https://grimzone.vercel.app",
      stats: { activePlayers: "348+", tournaments: "4", prizesAwarded: "50K+", rating: "4.9" },
      gameModes: ["1v1 Solo", "2v2 Duo"],
      entryFee: "50 FF",
      prize: "80 FF per tournament",
      features: ["Live Tournaments", "World Chat", "Rules & Guidelines", "Login/Register System", "Slot-Based Match System"],
      support: { whatsapp: "+92XXXXXXXXX", email: "support@grimzone.pk" }
    }
  ],
  stats: {
    yearsExperience: "4+",
    projectsDelivered: "30+",
    happyClients: "20+"
  }
};

function normalize(str) {
  return str.toLowerCase().replace(/[^a-z0-9\s]/g, '').trim();
}

function getGreeting() {
  const greetings = [
    "Hey there! 👋 I'm Abubakkar's AI assistant. Ask me about his projects like <b>Black Wolf Media</b>, <b>Zyro</b>, or <b>GrimZone</b> — or his skills, experience, and more!",
    "Hi! I can tell you all about Abubakkar — his projects, skills, experience, and background. Try asking about <span class='highlight'>Black Wolf Media</span>, <span class='highlight'>Zyro</span>, or <span class='highlight'>GrimZone</span>!",
    "Hello! I'm here to help you learn about Abubakkar. Curious about <b>Black Wolf Media</b> (video production agency), <b>Zyro</b> (AI web platform), or <b>GrimZone</b> (Free Fire tournaments)? Just ask!"
  ];
  return greetings[Math.floor(Math.random() * greetings.length)];
}

function getFallback() {
  const fallbacks = [
    "I'm not sure I understand. You can ask me about Abubakkar's projects (<b>Black Wolf Media</b>, <b>Zyro</b>, <b>GrimZone</b>), skills, experience, or contact info!",
    "Hmm, I don't have an answer for that. Try asking: <i>'Tell me about Black Wolf Media'</i>, <i>'What does Zyro do?'</i>, or <i>'How does GrimZone work?'</i>",
    "I couldn't find that in my knowledge base. Feel free to ask about his <b>projects</b>, <b>skills</b>, <b>experience</b>, or <b>background</b>!"
  ];
  return fallbacks[Math.floor(Math.random() * fallbacks.length)];
}

function findAnswer(query) {
  const q = normalize(query);

  // Greetings
  if (/^(hi|hello|hey|howdy|sup|yo|assalam)\b/.test(q)) {
    return getGreeting();
  }

  // Name
  if (/\b(name|who are you|who is)\b/.test(q) && /(you|this)/.test(q)) {
    return `I'm an AI assistant here to tell you about <span class="highlight">Abubakkar</span> — a full-stack developer and creative problem solver. Ask me about his projects like Black Wolf Media, Zyro, or GrimZone!`;
  }

  // Who is / about
  if (/\b(who is|tell me about|about)\b/.test(q) && /\b(Abubakkar|him|he|his|you)\b/.test(q)) {
    return `<span class="highlight">Abubakkar</span> is a Full-Stack Developer & Creative Problem Solver with 4+ years of experience. He specializes in React, Node.js, and TypeScript. He's built <b>Black Wolf Media</b> (video production agency), <b>Zyro</b> (AI web platform), and <b>GrimZone</b> (Free Fire tournaments). He's delivered 30+ projects for 20+ happy clients.`;
  }

  // ========== PROJECTS: GENERAL ==========
  if (/\b(project|build|made|create|portfolio|websites?|sites?|work)\b/.test(q) && !/\b(experience|job|company)\b/.test(q)) {
    let proj = `<span class="highlight">Abubakkar's</span> featured projects:\n\n`;
    knowledgeBase.projects.forEach(p => {
      proj += `<b>${p.name}</b>\n${p.tagline}\n${p.description}\n🔗 ${p.url}\n\n`;
    });
    return proj.trim();
  }

  // ========== PROJECT: BLACK WOLF MEDIA ==========
  if (/\b(black.?wolf|wolf.?media|blackwolf|bwm|video.?editing.?agency)\b/i.test(q)) {
    const p = knowledgeBase.projects[0];
    let answer = `<b>${p.name}</b> — ${p.tagline}\n\n${p.longDescription}\n\n`;
    answer += `📍 <b>Location:</b> ${p.location}\n`;
    answer += `📅 <b>Founded:</b> ${p.founded}\n`;
    answer += `📊 <b>Stats:</b> ${p.stats.projects} projects | ${p.stats.years} years | ${p.stats.clients} clients\n`;
    answer += `💰 <b>Pricing:</b> ${p.pricing}\n`;
    answer += `⏱️ <b>Turnaround:</b> ${p.turnaround}\n`;
    answer += `🌐 <b>Languages:</b> ${p.languages}\n`;
    answer += `🔗 <b>Website:</b> ${p.url}`;
    return answer;
  }

  // Black Wolf Media services
  if ((/\b(black.?wolf|wolf.?media|blackwolf|bwm)\b/i.test(q) || /\b(video.?editing.?agency)\b/i.test(q)) && /\b(service|offer|do|provide|what)\b/.test(q)) {
    const p = knowledgeBase.projects[0];
    return `<b>${p.name}</b> offers these services:\n\n` + p.services.map(s => `• ${s}`).join('\n') + `\n\n🔗 ${p.url}`;
  }

  // Black Wolf Media testimonials
  if ((/\b(black.?wolf|wolf.?media)\b/i.test(q) || /\b(bwm)\b/i.test(q)) && /\b(testimonial|review|client.*say|feedback|rating)\b/.test(q)) {
    const p = knowledgeBase.projects[0];
    return `<b>${p.name}</b> client testimonials:\n\n` + p.testimonials.map(t => `⭐ <b>${t.name}</b> (${t.role}): "${t.text}"`).join('\n\n');
  }

  // Black Wolf Media journey / history
  if ((/\b(black.?wolf|wolf.?media|bwm)\b/i.test(q) || /\b(video.?editing.?agency)\b/i.test(q)) && /\b(journey|history|timeline|found|started|milestone)\b/.test(q)) {
    const p = knowledgeBase.projects[0];
    return `<b>${p.name}</b> journey:\n\n` + p.journey.map(j => `<b>${j.year}</b> — ${j.milestone}`).join('\n\n');
  }

  // Black Wolf Media industries
  if ((/\b(black.?wolf|wolf.?media|bwm)\b/i.test(q) || /\b(video.?editing.?agency)\b/i.test(q)) && /\b(industr|sector|client.*type|cater)\b/.test(q)) {
    const p = knowledgeBase.projects[0];
    return `<b>${p.name}</b> serves these industries:\n\n` + p.industries.map(i => `• ${i}`).join('\n') + `\n\nThey partner with startups, SMEs, enterprises, and Pakistan government entities.`;
  }

  // Black Wolf Media tech / built with
  if ((/\b(black.?wolf|wolf.?media|bwm)\b/i.test(q) || /\b(video.?editing.?agency)\b/i.test(q)) && /\b(tech|built|stack|framework|tool|how.*made)\b/.test(q)) {
    const p = knowledgeBase.projects[0];
    return `<b>${p.name}</b> was built with: ${p.tech.join(', ')}.\n\n🔗 ${p.url}`;
  }

  // ========== PROJECT: ZYRO ==========
  if (/\b(zyro|alvixor)\b/i.test(q)) {
    const p = knowledgeBase.projects[1];
    let answer = `<b>${p.name}</b> — ${p.tagline}\n\n${p.longDescription}\n\n`;
    answer += `🛠️ <b>Tech Stack:</b> ${p.tech.join(', ')}\n`;
    answer += `🔗 <b>Website:</b> ${p.url}`;
    return answer;
  }

  // Zyro services / what they build
  if (/\b(zyro)\b/i.test(q) && /\b(service|offer|do|provide|build|what)\b/.test(q)) {
    const p = knowledgeBase.projects[1];
    let answer = `<b>${p.name}</b> builds:\n\n`;
    p.whatWeBuild.forEach(w => {
      answer += `<b>${w.name}</b>\n${w.desc}\n\n`;
    });
    answer += `Services: ${p.services.join(', ')}`;
    return answer.trim();
  }

  // Zyro process / how they work
  if (/\b(zyro)\b/i.test(q) && /\b(process|how.*work|phase|approach|method)\b/.test(q)) {
    const p = knowledgeBase.projects[1];
    let answer = `<b>${p.name}</b> follows a 3-phase approach:\n\n`;
    p.process.forEach(ph => {
      answer += `<b>Phase ${ph.phase}: ${ph.name}</b>\n${ph.desc}\n\n`;
    });
    return answer.trim();
  }

  // Zyro tech stack
  if (/\b(zyro)\b/i.test(q) && /\b(tech|stack|built|framework|tool)\b/.test(q)) {
    const p = knowledgeBase.projects[1];
    return `<b>${p.name}</b> tech stack: ${p.tech.join(', ')}.\n\n🔗 ${p.url}`;
  }

  // ========== PROJECT: GRIMZONE ==========
  if (/\b(grim.?zone|grimzone|free.?fire|tournament.?platform)\b/i.test(q)) {
    const p = knowledgeBase.projects[2];
    let answer = `<b>${p.name}</b> — ${p.tagline}\n\n${p.longDescription}\n\n`;
    answer += `👥 <b>Active Players:</b> ${p.stats.activePlayers}\n`;
    answer += `🏆 <b>Tournaments:</b> ${p.stats.tournaments}\n`;
    answer += `💰 <b>Prizes Awarded:</b> ${p.stats.prizesAwarded}\n`;
    answer += `⭐ <b>Rating:</b> ${p.stats.rating}\n`;
    answer += `🎮 <b>Game Modes:</b> ${p.gameModes.join(', ')}\n`;
    answer += `💵 <b>Entry Fee:</b> ${p.entryFee} | <b>Prize:</b> ${p.prize}\n`;
    answer += `🔗 <b>Website:</b> ${p.url}`;
    return answer;
  }

  // GrimZone game modes
  if (/\b(grim.?zone|grimzone|free.?fire)\b/i.test(q) && /\b(mode|game|type|play|1v1|2v2|solo|duo)\b/.test(q)) {
    const p = knowledgeBase.projects[2];
    return `<b>${p.name}</b> game modes:\n\n` + p.gameModes.map(m => `• ${m}`).join('\n') + `\n\nEntry fee: ${p.entryFee} | Prize: ${p.prize}`;
  }

  // GrimZone features
  if (/\b(grim.?zone|grimzone|free.?fire)\b/i.test(q) && /\b(feature|what.*have|offer|capabilities)\b/.test(q)) {
    const p = knowledgeBase.projects[2];
    return `<b>${p.name}</b> features:\n\n` + p.features.map(f => `• ${f}`).join('\n') + `\n\nSupport: WhatsApp ${p.support.whatsapp} | Email: ${p.support.email}`;
  }

  // GrimZone support
  if (/\b(grim.?zone|grimzone|free.?fire)\b/i.test(q) && /\b(contact|support|help|email|whatsapp)\b/.test(q)) {
    const p = knowledgeBase.projects[2];
    return `<b>${p.name}</b> support:\n\n📧 Email: ${p.support.email}\n💬 WhatsApp: ${p.support.whatsapp}`;
  }

  // GrimZone tech
  if (/\b(grim.?zone|grimzone|free.?fire)\b/i.test(q) && /\b(tech|built|stack|framework|tool)\b/.test(q)) {
    const p = knowledgeBase.projects[2];
    return `<b>${p.name}</b> was built with: ${p.tech.join(', ')}.\n\n🔗 ${p.url}`;
  }

  // ========== SKILLS ==========
  if (/\b(skill|tech|stack|technolog|know|language|tool|expert)\b/.test(q)) {
    return `<span class="highlight">Abubakkar's</span> tech stack includes:\n\n` +
      `🔷 <b>Frontend:</b> ${knowledgeBase.skills.frontend.join(', ')}\n` +
      `🖥️ <b>Backend:</b> ${knowledgeBase.skills.backend.join(', ')}\n` +
      `☁️ <b>DevOps:</b> ${knowledgeBase.skills.devops.join(', ')}\n` +
      `📱 <b>Mobile:</b> ${knowledgeBase.skills.mobile.join(', ')}\n` +
      `🔧 <b>Tools:</b> ${knowledgeBase.skills.tools.join(', ')}`;
  }

  if (/\bfrontend\b|\bfront-end\b/.test(q)) {
    return `<span class="highlight">Abubakkar's</span> frontend skills: ${knowledgeBase.skills.frontend.join(', ')}.`;
  }

  if (/\bback\s*end\b|\bbackend\b/.test(q)) {
    return `<span class="highlight">Abubakkar's</span> backend skills: ${knowledgeBase.skills.backend.join(', ')}.`;
  }

  // Specific tech questions
  if (/\breact\b/.test(q) && /\b(skill|know|use|work|experience)\b/.test(q)) {
    return `<span class="highlight">Abubakkar</span> is highly skilled in React and uses it as his primary frontend framework. He's built complex SPAs, dashboards, and interactive UIs with React, Next.js, and React Native. He used React in all three of his projects: Black Wolf Media, Zyro, and GrimZone.`;
  }

  if (/\bnode\b/.test(q) && /\b(skill|know|use|work|experience)\b/.test(q)) {
    return `<span class="highlight">Abubakkar</span> uses Node.js extensively for backend development — REST APIs, microservices, real-time applications with WebSockets, and CLI tools.`;
  }

  if (/\btypescript\b/.test(q)) {
    return `<span class="highlight">Abubakkar</span> uses TypeScript daily across both frontend and backend for type-safe, maintainable code.`;
  }

  if (/\bdocker\b/.test(q)) {
    return `<span class="highlight">Abubakkar</span> uses Docker for containerization and development environments. He also works with Kubernetes for orchestration.`;
  }

  if (/\b(aws|cloud|deploy)\b/.test(q)) {
    return `<span class="highlight">Abubakkar</span> has experience with AWS services including EC2, Lambda, S3, and RDS for cloud deployment and infrastructure.`;
  }

  // ========== EXPERIENCE ==========
  if (/\b(experience|work|job|career|employ|position|role|company)\b/.test(q)) {
    let exp = `<span class="highlight">Abubakkar's</span> work experience:\n\n`;
    knowledgeBase.experience.forEach(e => {
      exp += `<b>${e.role}</b> @ ${e.company} (${e.period})\n${e.description}\n\n`;
    });
    return exp.trim();
  }

  if (/\b(current|now|present|today|working on)\b/.test(q)) {
    const current = knowledgeBase.experience[0];
    return `Currently, <span class="highlight">Abubakkar</span> is working as <b>${current.role}</b> at ${current.company}. ${current.description}`;
  }

  if (/\b(how long|years|experience)\b/.test(q) && /\b(work|coding|dev|job)\b/.test(q)) {
    return `<span class="highlight">Abubakkar</span> has <b>4+ years</b> of professional development experience.`;
  }

  // ========== CONTACT ==========
  if (/\b(contact|email|reach|message|connect|hire|get in touch)\b/.test(q)) {
    return `You can reach <span class="highlight">Abubakkar</span> at:\n\n📧 <b>Email:</b> hello@Abubakkar.dev\n🐦 <b>Twitter:</b> @Abubakkar_dev\n🐙 <b>GitHub:</b> github.com/Abubakkar\n🔗 <b>LinkedIn:</b> linkedin.com/in/Abubakkar\n\nHe's based remotely and open to interesting opportunities worldwide!`;
  }

  if (/\b(location|where|based|remote|live|located)\b/.test(q)) {
    return `<span class="highlight">Abubakkar</span> works remotely from anywhere in the world. 🌍`;
  }

  // ========== STATS ==========
  if (/\b(stat|achievement|accomplish|deliver|client|project.*count|how many)\b/.test(q)) {
    return `<span class="highlight">Abubakkar's</span> stats:\n\n📦 <b>${knowledgeBase.stats.projectsDelivered}</b> projects delivered\n😊 <b>${knowledgeBase.stats.happyClients}</b> happy clients\n💼 <b>${knowledgeBase.stats.yearsExperience}</b> years experience`;
  }

  // ========== INTERESTS ==========
  if (/\b(coffee|hobby|interest|fun|outside|free time)\b/.test(q)) {
    return `When he's not coding, <span class="highlight">Abubakkar</span> enjoys exploring new frameworks, contributing to open-source projects, and perfecting his pour-over coffee technique! ☕`;
  }

  // ========== HELP ==========
  if (/\b(help|what can you|what do you know|capabilities|suggest|question)\b/.test(q)) {
    return "I can tell you about <span class='highlight'>Abubakkar's</span> projects, skills, work experience, contact info, and background. Try asking:\n\n" +
      "• <i>'Tell me about Black Wolf Media'</i>\n" +
      "• <i>'What does Zyro do?'</i>\n" +
      "• <i>'How does GrimZone work?'</i>\n" +
      "• <i>'What skills does Abubakkar have?'</i>\n" +
      "• <i>'Show me his projects'</i>";
  }

  // ========== GOODBYE ==========
  if (/\b(bye|goodbye|see you|thanks|thank you)\b/.test(q)) {
    return "You're welcome! Feel free to ask more about <span class='highlight'>Abubakkar</span> anytime. 😊";
  }

  return null;
}

class ChatbotAI {
  constructor() {
    this.apiKey = localStorage.getItem('gemini_api_key') || '';
    this.useAI = false;
    this.body = document.getElementById('chatbotBody');
    this.input = document.getElementById('chatbotInput');
    this.sendBtn = document.getElementById('chatbotSend');
    this.fab = document.getElementById('chatbotFab');
    this.closeBtn = document.getElementById('chatbotClose');
    this.overlay = document.getElementById('chatbotOverlay');
    this.chatbot = document.getElementById('chatbot');
    this.suggestionChips = document.getElementById('suggestionChips');
  }

  init() {
    this.sendBtn.addEventListener('click', () => this.handleSend());
    this.input.addEventListener('keydown', (e) => {
      if (e.key === 'Enter') this.handleSend();
    });
    this.fab.addEventListener('click', () => this.toggle());
    this.closeBtn.addEventListener('click', () => this.close());
    this.overlay.addEventListener('click', () => this.close());

    this.suggestionChips.querySelectorAll('.chip').forEach(chip => {
      chip.addEventListener('click', () => {
        this.input.value = chip.dataset.query;
        this.handleSend();
      });
    });

    document.getElementById('chatCTA')?.addEventListener('click', () => {
      this.open();
      this.input.focus();
    });
  }

  toggle() {
    if (this.chatbot.classList.contains('active')) {
      this.close();
    } else {
      this.open();
    }
  }

  open() {
    this.chatbot.classList.add('active');
    this.overlay.classList.add('active');
    this.fab.classList.add('open');
    this.fab.querySelector('.fab-icon').textContent = '✕';
    this.scrollToBottom();
    setTimeout(() => this.input.focus(), 300);
  }

  close() {
    this.chatbot.classList.remove('active');
    this.overlay.classList.remove('active');
    this.fab.classList.remove('open');
    this.fab.querySelector('.fab-icon').textContent = '💬';
  }

  handleSend() {
    const msg = this.input.value.trim();
    if (!msg) return;
    this.input.value = '';
    this.addMessage(msg, 'user');

    const loadingId = this.showTyping();

    setTimeout(() => {
      this.hideTyping(loadingId);
      const answer = this.getAnswer(msg);
      this.addMessage(answer, 'bot');
    }, 400 + Math.random() * 300);
  }

  getAnswer(query) {
    const local = findAnswer(query);
    if (local) return local;

    if (this.apiKey) {
      this.fetchAIResponse(query, this.apiKey);
      return "Let me think about that...";
    }

    return getFallback();
  }

  addMessage(text, sender) {
    const div = document.createElement('div');
    div.className = `chat-message ${sender}`;

    const avatar = document.createElement('div');
    avatar.className = 'chat-avatar';
    avatar.textContent = sender === 'user' ? '👤' : '🤖';

    const bubble = document.createElement('div');
    bubble.className = 'chat-bubble';
    bubble.innerHTML = text;

    div.appendChild(avatar);
    div.appendChild(bubble);
    this.body.appendChild(div);
    this.scrollToBottom();
  }

  showTyping() {
    const id = 'typing-' + Date.now();
    const div = document.createElement('div');
    div.className = 'chat-message bot';
    div.id = id;

    const avatar = document.createElement('div');
    avatar.className = 'chat-avatar';
    avatar.textContent = '🤖';

    const bubble = document.createElement('div');
    bubble.className = 'chat-bubble';
    bubble.innerHTML = '<div class="chat-typing"><span></span><span></span><span></span></div>';

    div.appendChild(avatar);
    div.appendChild(bubble);
    this.body.appendChild(div);
    this.scrollToBottom();
    return id;
  }

  hideTyping(id) {
    const el = document.getElementById(id);
    if (el) el.remove();
  }

  scrollToBottom() {
    setTimeout(() => {
      this.body.scrollTop = this.body.scrollHeight;
    }, 50);
  }

  async fetchAIResponse(query, apiKey) {
    this.hideTyping(this._loadingId);

    const context = this.buildContext();
    const prompt = `You are an AI assistant for a portfolio website. You represent ${knowledgeBase.fullName}. Answer questions about him based on this context. Be friendly, concise, and use "he/him" when referring to ${knowledgeBase.fullName}. If unsure, say you don't know.\n\nContext:\n${context}\n\nQuestion: ${query}`;

    try {
      const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.0-flash:generateContent?key=${apiKey}`, {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({
          contents: [{ parts: [{ text: prompt }] }]
        })
      });

      const data = await response.json();
      if (data?.candidates?.[0]?.content?.parts?.[0]?.text) {
        this.addMessage(data.candidates[0].content.parts[0].text, 'bot');
      } else {
        this.addMessage(getFallback(), 'bot');
      }
    } catch (err) {
      this.addMessage("I'm having trouble connecting to the AI service. Here's what I know: " + getFallback(), 'bot');
    }
  }

  buildContext() {
    return JSON.stringify(knowledgeBase, null, 2);
  }

  setApiKey(key) {
    this.apiKey = key;
    localStorage.setItem('gemini_api_key', key);
  }
}

const chatbotAI = new ChatbotAI();
document.addEventListener('DOMContentLoaded', () => chatbotAI.init());
